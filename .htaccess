<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route all requests through the public directory when docroot is project root.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
